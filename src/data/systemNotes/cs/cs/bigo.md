Big O는 알고리즘의 시간/공간 복잡도를 나타내는 표기법입니다.

## 주요 복잡도 (빠른 순)

- **O(1)**: 상수 시간
- **O(log n)**: 로그 시간 (이진 탐색)
- **O(n)**: 선형 시간 (배열 순회)
- **O(n log n)**: 선형 로그 시간 (병합 정렬)
- **O(n²)**: 이차 시간 (중첩 반복문)
- **O(2ⁿ)**: 지수 시간 (피보나치 재귀)

## 평가 기준

- 최악의 경우를 기준으로 평가
- 상수와 낮은 차수 항은 무시
- 가장 큰 차수만 고려
