TypeScript의 기본 타입 시스템입니다.

## 원시 타입

- string, number, boolean
- null, undefined
- symbol, bigint

## 특수 타입

- **any**: 모든 타입 허용 (지양해야 함)
- **unknown**: 타입 안전한 any
- **void**: 반환값 없음
- **never**: 절대 발생하지 않는 값

## 배열과 튜플

- `number[]`, `Array<number>`
- `[string, number]`: 고정된 길이와 타입
