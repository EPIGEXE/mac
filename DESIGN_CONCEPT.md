# 맥 (MAK) - 디자인 컨셉 문서

## 컨셉: 개발자의 터미널 (Developer's Terminal)

개발자 면접 노트 앱으로서, 터미널과 코드 에디터에서 영감을 받은 미니멀하고 기술적인 디자인을 추구합니다.

---

## 타이포그래피 시스템

### 폰트 패밀리

| 용도 | 폰트 | CSS 변수 | 설명 |
|------|------|----------|------|
| 본문 | Pretendard | `--font-system` | 가독성이 뛰어난 한글 산세리프 |
| 코드/UI | D2Coding | `--font-mono` | 개발자 친화적 모노스페이스 |

### 사용 가이드

```css
/* 본문, 설명 텍스트 */
font-family: var(--font-system);

/* 코드, 터미널 스타일 UI, 태그, 카운터 */
font-family: var(--font-mono);
```

---

## UI 디자인 원칙

### 1. 터미널 스타일 표기법

코드/터미널에서 영감을 받은 표기법을 사용합니다:

```
// 주석 스타일 서브타이틀
view: list | map    (명령어 스타일 토글)
*all, cs, fe, be    (접두사 스타일 필터)
[3]                 (배열 인덱스 스타일 카운트)

등등 필요한 UI에 코드/터미널 스타일 적용
```

### 2. 인터랙션 패턴

- **언더라인 인디케이터**: 배경색 대신 하단 보더로 활성 상태 표시
- **호버 효과**: 미묘한 배경색 변화 (`var(--bg-hover)`)
- **색상 변화**: 비활성(`--text-tertiary`) → 활성(`--accent`)

### 3. 컬러 사용

```css
/* 주요 액센트 */
--accent: #6366f1 (라이트) / #8b5cf6 (다크)

/* 텍스트 계층 */
--text-primary: 제목, 중요 콘텐츠
--text-secondary: 본문, 설명
--text-tertiary: 메타정보, 비활성 UI
```

---

## 컴포넌트 스타일 가이드

### 헤더

```
┌─────────────────────────────────────────────────────┐
│  맥                           view: list | map  ◐   │
│  // 개발자 면접 노트                                  │
├─────────────────────────────────────────────────────┤
│  *전체   cs CS   fe 프론트엔드   be 백엔드           │
│  ═══════                                            │
└─────────────────────────────────────────────────────┘
```

- 서브타이틀: `D2Coding`, 12px, 주석 스타일 (`//`)
- 토글: 터미널 명령어 스타일 (`view: list | map`)
- 필터 탭: 모노 접두사 + 라벨

### 리스트 아이템

```
┌─────────────────────────────────────────────────────┐
│  # Category                               [3] + new │
├─────────────────────────────────────────────────────┤
│  > 노트 제목                          tag1  tag2    │
│    첫 번째 줄 미리보기...                            │
├ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┤
│  > 노트 제목 2                              tag3    │
│    첫 번째 줄 미리보기...                            │
└─────────────────────────────────────────────────────┘
```

- 카테고리 헤더: `#` 접두사, 모노스페이스
- 노트 개수: `[n]` 배열 스타일
- 새 노트 버튼: `+ new` 터미널 스타일
- 노트 제목: `>` 접두사로 선택 가능함을 암시
- 태그: 모노스페이스, 작은 크기

---

## 애니메이션 & 트랜지션

```css
/* 기본 이징 */
transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);

/* 표준 지속시간 */
--duration-fast: 0.15s;
--duration-normal: 0.2s;
--duration-slow: 0.3s;
```

---

## 다크 모드

다크 모드에서는 보라색 계열의 액센트 컬러를 사용하여 터미널의 야간 모드 느낌을 줍니다:

- Light: Indigo (`#6366f1`)
- Dark: Purple (`#8b5cf6`)

---

## 디자인 철학

1. **미니멀리즘**: 불필요한 장식 배제, 콘텐츠 중심
2. **개발자 친화적**: 터미널/코드 에디터에서 영감받은 UI
3. **일관성**: 모든 컴포넌트에서 동일한 디자인 언어 사용
4. **가독성**: 면접 준비라는 목적에 맞게 읽기 쉬운 타이포그래피
