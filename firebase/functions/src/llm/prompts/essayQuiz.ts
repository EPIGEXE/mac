// ==================================== 서술형 문제 생성 프롬프트 ================================
interface EssayQuizPromptParams {
    title: string
    content: string
}

// 한국 테크 기업 면접관 스타일
const KOREAN_TECH_COMPANIES = [
    {
        name: '네이버',
        style: '내부 동작 원리를 깊이 파고드는 질문',
        example: '"이게 내부적으로 어떻게 동작하나요?", "왜 이런 방식으로 구현되어 있을까요?"',
    },
    {
        name: '카카오',
        style: '실제 문제 해결 경험과 과정을 묻는 질문',
        example: '"이 기술을 실제로 적용해본 경험이 있나요?", "어떤 문제를 해결하셨나요?"',
    },
    {
        name: '쿠팡',
        style: '성능과 확장성 관점의 질문',
        example: '"이 방식의 시간/공간 복잡도는?", "병목이 생긴다면 어디서 생길까요?"',
    },
    {
        name: '토스',
        style: '설계 결정과 트레이드오프를 묻는 질문',
        example: '"왜 이 방식을 선택하셨나요?", "다른 대안은 고려해보셨나요?"',
    },
    {
        name: '당근',
        style: '실용적 관점과 사용자 영향을 묻는 질문',
        example: '"이게 사용자에게 어떤 영향을 주나요?", "더 간단한 방법은 없을까요?"',
    },
]

// 서술형 문제 생성 프롬프트
export function buildEssayQuizPrompt(params: EssayQuizPromptParams): string {
    const { title, content } = params

    // 랜덤으로 회사 선택
    const company = KOREAN_TECH_COMPANIES[Math.floor(Math.random() * KOREAN_TECH_COMPANIES.length)]

    return `Task: 한국 테크 기업의 시니어 개발자로서 **실무 시나리오 기반** 기술 면접 질문을 생성하세요.

## 면접관 페르소나
- **회사**: ${company.name}
- **역할**: 5년차+ 시니어 개발자
- **면접 스타일**: ${company.style}

## 지원자가 준비한 학습 노트
제목: ${title}
내용:
${content}

---

## 면접 질문 생성 가이드

### 핵심 원칙: 실무 시나리오 + Deep Dive

단순히 "~가 뭔가요?"가 아닌, **구체적인 상황을 제시하고 해결 방안을 묻는** 질문을 만드세요.

**좋은 질문 구조:**
\`\`\`
[실무 상황/제약 조건] + [노트 주제 적용] + [해결/개선 방안 요청]
\`\`\`

**예시 (노트 주제: React 렌더링 최적화):**
- ❌ 나쁜 질문: "React에서 불필요한 리렌더링을 방지하는 방법은?"
- ✅ 좋은 질문: "대시보드 페이지에서 실시간 데이터가 1초마다 갱신되는데, 차트 컴포넌트만 업데이트되어야 하는 상황입니다. 그런데 전체 페이지가 리렌더링되면서 사용자 입력 폼의 상태가 초기화되는 문제가 발생했어요. 이 상황을 어떻게 개선하시겠어요?"

**예시 (노트 주제: HTTP 캐싱):**
- ❌ 나쁜 질문: "Cache-Control 헤더의 종류를 설명해주세요"
- ✅ 좋은 질문: "이커머스 상품 상세 페이지에서 가격은 실시간이어야 하지만, 상품 이미지와 설명은 자주 안 바뀝니다. 하나의 API에서 모든 데이터를 내려주는 상황에서, 캐싱 전략을 어떻게 설계하시겠어요?"

### 질문 유형별 시나리오 패턴

**troubleshooting (장애/버그 상황)**
- "프로덕션에서 [증상]이 발생했습니다. [제약조건]인 상황에서 원인을 어떻게 파악하고 해결하시겠어요?"

**tradeoff (설계 결정)**
- "[요구사항 A]와 [요구사항 B]가 충돌하는 상황입니다. 어떤 기준으로 결정하시겠어요?"

**application (적용/구현)**
- "[비즈니스 요구사항]을 만족시키려면 [노트 주제]를 어떻게 활용하시겠어요?"

**concept (원리 심화)**
- "[특정 상황]에서 내부적으로 어떤 일이 일어나나요? 왜 그렇게 동작하도록 설계되었을까요?"

### 꼬리질문 작성

메인 질문의 답변을 **더 깊이 파고드는** 방향:
- 답변에서 언급한 해결책의 **한계나 예외 케이스**를 물음
- "그 방식의 단점은요?", "만약 [엣지케이스]라면?"
- 트레이드오프를 인지하고 있는지 확인

### 평가 기준 (evaluationCriteria) - 중요!

질문의 난이도에 맞는 **구체적인 평가 기준**을 작성하세요:
- 각 기준은 **"~를 설명했는가"** 형태의 체크 가능한 항목
- 단순 키워드 매칭이 아닌, **논리적 흐름과 근거**를 평가할 수 있는 기준
- 상/중/하 수준을 구분할 수 있는 기준 포함

---

## Output Format
Return ONLY valid JSON:
{
  "company": "${company.name}",
  "question": "실무 시나리오가 포함된 면접 질문. 반드시 [구체적 상황 설명 2-3문장] + [해결 방안을 묻는 질문]으로 구성. 예시 수준의 구체성 필수 (한국어, 존댓말)",
  "questionType": "concept | comparison | application | troubleshooting | tradeoff",
  "scenario": {
    "situation": "구체적인 실무 상황 (1-2문장)",
    "constraint": "제약 조건이나 요구사항",
    "challenge": "해결해야 할 핵심 문제"
  },
  "followUpQuestions": [
    "답변의 한계/예외를 파고드는 꼬리질문 1",
    "트레이드오프나 대안을 묻는 꼬리질문 2"
  ],
  "expectedPoints": [
    "반드시 언급해야 할 핵심 개념/원리 1",
    "반드시 언급해야 할 핵심 개념/원리 2",
    "반드시 언급해야 할 핵심 개념/원리 3"
  ],
  "evaluationCriteria": {
    "excellent": ["90점+ 답변이 갖춰야 할 조건 1", "90점+ 조건 2"],
    "good": ["70-89점 답변의 기준 1", "70-89점 기준 2"],
    "poor": ["이것이 빠지면 감점되는 항목 1", "감점 항목 2"]
  },
  "answerGuide": "좋은 답변의 구조와 논리 전개 방향 (2-3문장)"
}`
}


// ==================================== 서술형 답변 평가 프롬프트 ================================
interface EvaluateEssayAnswerParams {
    company: string
    question: string
    questionType: string
    expectedPoints: string[]
    evaluationCriteria?: {
        excellent: string[]
        good: string[]
        poor: string[]
    }
    answerGuide: string
    userAnswer: string
}

// 서술형 답변 평가 프롬프트
export function buildEvaluateEssayPrompt(params: EvaluateEssayAnswerParams): string {
    const { company, question, questionType, expectedPoints, evaluationCriteria, answerGuide, userAnswer } = params

    // evaluationCriteria가 있으면 상세 기준 사용, 없으면 기본 포인트 기반
    const criteriaSection = evaluationCriteria
        ? `
## 등급별 평가 기준

**🏆 Excellent (90-100점) - 다음을 모두 충족:**
${evaluationCriteria.excellent.map((c) => `- ${c}`).join('\n')}

**✅ Good (70-89점) - 다음을 대부분 충족:**
${evaluationCriteria.good.map((c) => `- ${c}`).join('\n')}

**⚠️ Poor (70점 미만) - 다음 중 하나라도 해당:**
${evaluationCriteria.poor.map((c) => `- ${c}`).join('\n')}
`
        : `
## 핵심 포인트 (반드시 언급해야 할 내용)
${expectedPoints.map((p, i) => `${i + 1}. ${p}`).join('\n')}
`

    return `Task: 한국 테크 기업 시니어 개발자로서 **실무 시나리오 기반 면접 질문**에 대한 답변을 평가하세요.

## 면접 정보
- **회사**: ${company}
- **질문**: ${question}
- **질문 유형**: ${questionType}
${criteriaSection}
## 모범 답변 방향
${answerGuide}

## 지원자 답변
${userAnswer}

---

## 평가 원칙

### 1. 질문 유형별 평가 관점 (중요!)

**질문 유형: ${questionType}**

질문 유형에 따라 평가 관점의 **가중치가 다릅니다**:

- **concept (개념 심화)**: 기술적 정확성(40%) + 동작 원리 설명(40%) + 예시/적용(20%). 트레이드오프는 필수 아님.
- **comparison (비교)**: 차이점 명확성(40%) + 각각의 장단점(30%) + 상황별 선택 기준(30%)
- **application (적용)**: 상황 이해(30%) + 해결책 적합성(40%) + 구현 구체성(30%)
- **troubleshooting (문제 해결)**: 원인 분석(40%) + 해결 접근법(40%) + 예방책(20%)
- **tradeoff (설계 결정)**: 대안 인식(30%) + 장단점 분석(40%) + 결정 근거(30%)

**중요**: concept 유형 질문에서 트레이드오프나 대안 제시가 없다고 감점하지 마세요. 해당 질문이 요구하는 것에 집중하세요.

### 2. 기술적 정확성 검증 (최우선)

아무리 논리적으로 설명해도, 다음 경우 **즉시 감점**:
- **오개념**: 기술의 동작 원리를 잘못 이해
- **안티패턴 권장**: 성능/안정성을 해치는 방법을 최선으로 제시
- **근거 없는 주장**: 검증되지 않은 성능 향상이나 효과 주장

### 3. 점수 산정 - 100점은 가능하다!

**중요**: 100점은 "완벽한 답변"이 아니라 **"질문이 요구하는 것을 충분히 충족한 답변"**입니다.
500자 제한 내에서 모든 것을 다 담을 수 없으므로, **핵심을 정확하고 충분히 설명했으면 100점**을 줄 수 있습니다.

**95-100점**: 질문의 핵심을 정확히 이해하고, 기술적으로 정확하며, 질문이 요구하는 수준의 깊이로 설명. 더 추가할 필요가 없음
**90-94점**: 핵심을 잘 설명했으나, 한두 가지 부연 설명이 있으면 더 완벽했을 답변
**85-89점**: 핵심 개념은 정확하나, 설명이 조금 더 구체적이거나 체계적일 수 있음
**80-84점**: 대부분 정확하나, 일부 설명이 부족하거나 약간의 보완 필요
**70-79점**: 기본 개념은 알지만, 깊이 있는 설명이나 구체적 예시 부족
**60-69점**: 개념 이해는 있으나 핵심을 놓치거나 설명이 부정확한 부분 존재
**0-59점**: 부분적 이해만 있거나, 오개념 포함, 질문 의도 파악 실패

**주의**:
- "트레이드오프 언급 없음"은 tradeoff/comparison 유형에서만 감점 요소
- "더 좋을 수 있었다"는 감점 이유가 아님. 핵심을 충족했으면 고득점 부여

### 4. 피드백 작성

- **구체적으로**: "좋았다/아쉬웠다"가 아닌, 어떤 부분이 왜 좋았고/아쉬웠는지
- **건설적으로**: 부족한 부분에 대해 어떻게 보완하면 좋을지 방향 제시
- **실무 관점에서**: 실제 면접에서 이런 답변이 어떻게 평가될지

---

## Output Format
Return ONLY valid JSON:
{
  "score": number (0-100),
  "grade": "PASS" | "BORDERLINE" | "NEEDS_WORK",
  "analysis": {
    "situationUnderstanding": "상황/제약조건을 얼마나 정확히 파악했는지 (1-2문장)",
    "solutionQuality": "제시한 해결책의 적합성과 실현 가능성 (1-2문장)",
    "technicalAccuracy": "기술적 정확성 평가 (오개념이 있다면 명시)",
    "depthOfThinking": "설명의 깊이와 구체성 수준. concept 유형은 '원리 설명의 깊이', tradeoff 유형은 '대안/장단점 분석 수준' 등 질문 유형에 맞게 평가 (1-2문장)"
  },
  "matchedPoints": ["충족한 핵심 포인트들"],
  "missedPoints": ["놓친 핵심 포인트들. 95점 이상이면 빈 배열 []"],
  "strengths": ["답변의 강점 1", "강점 2"],
  "improvements": ["95점 이상 = 빈 배열 []. 90-94점 = 선택적 보완사항 1개 (없어도 되는 수준). 80-89점 = 실제 부족한 부분 1-2개. 80점 미만 = 핵심적으로 놓친 부분"],
  "feedback": "면접관 종합 피드백 (3-4문장, 한국어 존댓말). 95점 이상이면 칭찬만, 90-94점은 칭찬 위주 + 선택적 보완, 80-89점은 균형있게, 80점 미만은 부족한 점 중심",
  "betterAnswer": "95점 이상 = 빈 문자열 (훌륭한 답변이므로 불필요). 90-94점 = 빈 문자열 또는 짧은 보완 제안. 80-89점 = 부족했던 부분만 보완한 예시 문장. 80점 미만 = 전체적인 모범 답변 예시"
}`
}