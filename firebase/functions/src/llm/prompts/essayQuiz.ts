// ==================================== 서술형 문제 생성 프롬프트 ================================
interface EssayQuizPromptParams {
    title: string
    content: string
}

// 한국 테크 기업 면접관 스타일 (회사명 우회 표현)
const KOREAN_TECH_COMPANIES = [
    {
        name: '초록창',
        style: '내부 동작 원리를 깊이 파고드는 질문. "이게 내부적으로 어떻게 동작하나요?", "왜 이런 방식으로 구현되어 있을까요?"',
    },
    {
        name: '노란방',
        style: '실제 문제 해결 경험과 과정을 묻는 질문. "이 기술을 실제로 적용해본 경험이 있나요?", "어떤 문제를 해결하셨나요?"',
    },
    {
        name: '로켓배송',
        style: '성능과 확장성 관점의 질문. "이 방식의 시간/공간 복잡도는?", "트래픽이 10배가 되면 어떻게 되나요?"',
    },
    {
        name: '파란통장',
        style: '설계 결정과 트레이드오프를 묻는 질문. "왜 이 방식을 선택하셨나요?", "다른 대안은 고려해보셨나요?"',
    },
    {
        name: '동네장터',
        style: '실용적 관점과 사용자 영향을 묻는 질문. "이게 사용자에게 어떤 영향을 주나요?", "더 간단한 방법은 없을까요?"',
    },
]

// 서술형 문제 생성 프롬프트
export function buildEssayQuizPrompt(params: EssayQuizPromptParams): string {
    const { title, content } = params

    // 랜덤으로 회사 선택
    const company = KOREAN_TECH_COMPANIES[Math.floor(Math.random() * KOREAN_TECH_COMPANIES.length)]

    return `Task: 한국 테크 기업의 시니어 개발자로서 **노트 주제가 실제로 마주칠 수 있는 실무 상황**에서 기술 면접 질문을 생성하세요.

## 면접관 페르소나
- **회사**: ${company.name}
- **역할**: 5년차+ 시니어 개발자
- **면접 스타일**: ${company.style}

## 지원자가 준비한 학습 노트
제목: ${title}
내용:
${content}

---

## 면접 질문 생성 가이드

### 🚨 가장 중요한 원칙: 노트 주제가 자연스럽게 적용되는 실무 시나리오

노트의 내용을 보고, **이 기술/개념이 실제로 필요한 상황**을 떠올려서 질문을 만드세요.

**질문 생성 프로세스:**
1. 노트 주제의 핵심 개념 파악
2. 이 개념이 **실제로 마주칠 수 있는 실무 상황** 도출
3. 그 상황에서 ${company.name} 스타일로 질문 구성

**절대 금지 (이런 질문은 0점 처리):**
- "~의 차이점을 설명하세요" → 단순 개념 비교는 빈칸 퀴즈용
- "~를 사용하여 ~를 향상시키는 방법" → 일반론은 면접 질문이 아님
- "~의 장단점을 설명하세요" → 어디서든 나올 수 있는 질문
- 실무 맥락 없이 개념만 묻는 질문
- **노트 주제를 억지로 끼워 맞춘 질문** → 부자연스러운 상황 설정

**좋은 질문의 필수 요소:**
1. **노트 주제가 실제로 필요한 구체적 상황** 제시
2. **그 상황 특유의 제약/요구사항** 포함
3. ${company.name} 면접 스타일에 맞는 질문 방식

**좋은 질문 구조:**
\`\`\`
[노트 주제가 필요한 구체적 상황 2-3문장] + [${company.name} 스타일의 질문]
\`\`\`

**예시 (노트 주제: HTTP 캐싱):**
- ❌ 나쁜 질문: "Cache-Control 헤더의 종류를 설명해주세요" → 일반 개념 질문
- ✅ 좋은 질문: "상품 상세 페이지에서 가격은 실시간으로 바뀌지만, 상품 이미지와 설명은 거의 안 바뀝니다. 하나의 API에서 모든 데이터를 내려주는 상황에서, 캐싱 전략을 어떻게 설계하시겠어요?"

**예시 (노트 주제: React 상태 관리):**
- ❌ 나쁜 질문: "Redux와 Zustand의 차이점을 설명하세요" → 단순 비교
- ✅ 좋은 질문: "실시간 데이터가 1초마다 업데이트되는 화면에서, 동시에 사용자가 입력 폼을 작성 중입니다. 입력 상태가 실시간 업데이트에 영향받지 않으면서도 최신 데이터를 반영해야 한다면, 상태 관리를 어떻게 설계하시겠어요?"

### 질문 유형별 시나리오 패턴

**troubleshooting (장애/버그 상황)**
- "프로덕션에서 [증상]이 발생했습니다. [제약조건]인 상황에서 원인을 어떻게 파악하고 해결하시겠어요?"

**tradeoff (설계 결정)**
- "[요구사항 A]와 [요구사항 B]가 충돌하는 상황입니다. 어떤 기준으로 결정하시겠어요?"

**application (적용/구현)**
- "[비즈니스 요구사항]을 만족시키려면 [노트 주제]를 어떻게 활용하시겠어요?"

**concept (원리 심화)**
- "[특정 상황]에서 내부적으로 어떤 일이 일어나나요? 왜 그렇게 동작하도록 설계되었을까요?"

### 꼬리질문 작성

메인 질문의 답변을 **더 깊이 파고드는** 방향:
- 답변에서 언급한 해결책의 **한계나 예외 케이스**를 물음
- "그 방식의 단점은요?", "만약 [엣지케이스]라면?"
- 트레이드오프를 인지하고 있는지 확인

### 평가 기준 (expectedPoints, evaluationCriteria) - 매우 중요!

**🚨 핵심 원칙: 질문에서 명시적으로 요구한 내용만 평가 기준에 포함**

질문에서 물어보지 않은 내용을 평가 기준에 넣지 마세요:
- ❌ 질문: "단일 책임 원칙과 합성을 고려하여 설계해주세요" → 평가 기준에 "재시도 메커니즘", "캐싱 전략" 포함 (질문에 없는 내용)
- ✅ 질문: "단일 책임 원칙과 합성을 고려하여 설계해주세요" → 평가 기준에 "SRP를 적용한 컴포넌트 분리", "합성 패턴 활용" 포함

**expectedPoints 작성 규칙:**
- 질문에서 **직접 언급한 키워드/개념**에 대한 설명만 포함
- "bonus" 또는 "추가 점수" 형태로 질문 범위 외 내용을 넣지 말 것

**evaluationCriteria 작성 규칙:**
- excellent: 질문의 핵심을 **정확하고 구체적으로** 설명
- good: 질문의 핵심을 **기본적으로** 설명
- poor: 질문의 핵심을 **놓친** 경우

---

## Output Format
Return ONLY valid JSON:
{
  "company": "${company.name}",
  "question": "노트 주제가 실제로 필요한 구체적 실무 상황 기반 면접 질문. 반드시 [구체적 상황 2-3문장] + [${company.name} 스타일의 질문]으로 구성. 일반적 개념 비교/설명 질문은 절대 금지 (한국어, 존댓말)",
  "questionType": "concept | comparison | application | troubleshooting | tradeoff",
  "scenario": {
    "situation": "구체적인 실무 상황 (1-2문장)",
    "constraint": "제약 조건이나 요구사항",
    "challenge": "해결해야 할 핵심 문제"
  },
  "followUpQuestions": [
    "답변의 한계/예외를 파고드는 꼬리질문 1",
    "트레이드오프나 대안을 묻는 꼬리질문 2"
  ],
  "expectedPoints": [
    "반드시 언급해야 할 핵심 개념/원리 1",
    "반드시 언급해야 할 핵심 개념/원리 2",
    "반드시 언급해야 할 핵심 개념/원리 3"
  ],
  "evaluationCriteria": {
    "excellent": ["90점+ 답변이 갖춰야 할 조건 1", "90점+ 조건 2"],
    "good": ["70-89점 답변의 기준 1", "70-89점 기준 2"],
    "poor": ["이것이 빠지면 감점되는 항목 1", "감점 항목 2"]
  },
  "answerGuide": "좋은 답변의 구조와 논리 전개 방향 (2-3문장)"
}`
}


// ==================================== 서술형 답변 평가 프롬프트 ================================
interface EvaluateEssayAnswerParams {
    company: string
    question: string
    questionType: string
    expectedPoints: string[]
    evaluationCriteria?: {
        excellent: string[]
        good: string[]
        poor: string[]
    }
    answerGuide: string
    userAnswer: string
}

// 서술형 답변 평가 프롬프트
export function buildEvaluateEssayPrompt(params: EvaluateEssayAnswerParams): string {
    const { company, question, questionType, expectedPoints, evaluationCriteria, answerGuide, userAnswer } = params

    // evaluationCriteria가 있으면 상세 기준 사용, 없으면 기본 포인트 기반
    const criteriaSection = evaluationCriteria
        ? `
## 등급별 평가 기준

**🏆 Excellent (90-100점) - 다음을 모두 충족:**
${evaluationCriteria.excellent.map((c) => `- ${c}`).join('\n')}

**✅ Good (70-89점) - 다음을 대부분 충족:**
${evaluationCriteria.good.map((c) => `- ${c}`).join('\n')}

**⚠️ Poor (70점 미만) - 다음 중 하나라도 해당:**
${evaluationCriteria.poor.map((c) => `- ${c}`).join('\n')}
`
        : `
## 핵심 포인트 (반드시 언급해야 할 내용)
${expectedPoints.map((p, i) => `${i + 1}. ${p}`).join('\n')}
`

    return `Task: 한국 테크 기업 시니어 개발자로서 **실무 시나리오 기반 면접 질문**에 대한 답변을 평가하세요.

## 면접 정보
- **회사**: ${company}
- **질문**: ${question}
- **질문 유형**: ${questionType}
${criteriaSection}
## 모범 답변 방향
${answerGuide}

## 지원자 답변
${userAnswer}

---

## 평가 원칙

### 1. 질문 유형별 평가 관점 (중요!)

**질문 유형: ${questionType}**

질문 유형에 따라 평가 관점의 **가중치가 다릅니다**:

- **concept (개념 심화)**: 기술적 정확성(40%) + 동작 원리 설명(40%) + 예시/적용(20%). 트레이드오프는 필수 아님.
- **comparison (비교)**: 차이점 명확성(40%) + 각각의 장단점(30%) + 상황별 선택 기준(30%)
- **application (적용)**: 상황 이해(30%) + 해결책 적합성(40%) + 구현 구체성(30%)
- **troubleshooting (문제 해결)**: 원인 분석(40%) + 해결 접근법(40%) + 예방책(20%)
- **tradeoff (설계 결정)**: 대안 인식(30%) + 장단점 분석(40%) + 결정 근거(30%)

**중요**: concept 유형 질문에서 트레이드오프나 대안 제시가 없다고 감점하지 마세요. 해당 질문이 요구하는 것에 집중하세요.

### 2. 질문 범위 내에서만 평가 (매우 중요!)

**🚨 질문에서 명시적으로 요구하지 않은 내용은 평가하지 마세요.**

예시:
- 질문: "단일 책임 원칙과 합성을 고려하여 설계해주세요"
- ❌ 감점 사유로 사용 불가: "재시도 메커니즘 미언급", "캐싱 전략 부재", "에러 핸들링 미고려"
- ✅ 평가 대상: "SRP 적용 여부", "합성 패턴 활용 여부"

질문에 없는 내용을 언급하지 않았다고 감점하면 **절대 안 됩니다**.

### 3. 기술적 정확성 검증 (최우선)

아무리 논리적으로 설명해도, 다음 경우 **즉시 감점**:
- **오개념**: 기술의 동작 원리를 잘못 이해
- **안티패턴 권장**: 성능/안정성을 해치는 방법을 최선으로 제시
- **근거 없는 주장**: 검증되지 않은 성능 향상이나 효과 주장

### 4. 500자 제한을 고려한 평가 (매우 중요!)

지원자는 **500자 이내**로 답변해야 합니다. 이 제한 내에서:
- 핵심 개념 + 실무 예시 1개면 **충분**합니다
- 여러 예시, 모든 엣지케이스, 완벽한 설명을 기대하지 마세요
- **"예시가 더 있으면 좋겠다"는 500자 제한에서 불합리한 기대입니다**

### 5. 점수 산정 - 100점은 반드시 나와야 한다!

**핵심**: 100점은 "완벽한 답변"이 아니라 **"500자 내에서 질문의 핵심을 충분히 충족한 답변"**입니다.

**100점 기준**: 핵심 개념을 정확히 설명 + 기술적 오류 없음. 이 조건을 만족하면 **반드시 100점**
**95-99점**: 100점 조건을 거의 충족하나, 표현이 약간 아쉬운 경우
**90-94점**: 핵심은 정확하나, 설명의 논리 전개가 약간 부족
**80-89점**: 핵심 개념은 알지만, 설명이 피상적이거나 예시가 부족
**70-79점**: 기본 개념은 있으나 깊이 부족
**0-69점**: 오개념 포함 또는 질문 의도 파악 실패

**금지 사항 (감점 이유로 사용 불가)**:
- "예시가 더 있으면 좋겠다" → 500자 제한에서 예시 1개면 충분
- "트레이드오프 언급 없음" → concept 유형에서는 필수 아님
- "더 깊이 있는 설명" → 핵심을 정확히 설명했으면 충분

### 6. 피드백 작성 - 구체성이 핵심!

**🚨 절대 금지하는 피드백 패턴:**
- ❌ "더 구체적인 예시를 들 수 있습니다" → 무슨 예시를 들어야 하는지 직접 제시해야 함
- ❌ "추가적인 최적화 방법을 제시할 수 있습니다" → 어떤 최적화 방법인지 직접 언급해야 함
- ❌ "이러한 부분을 보완한다면 더 완벽할 것입니다" → 빈말, 내용 없음
- ❌ "~할 수 있습니다", "~하면 좋겠습니다" 형태의 모호한 제안

**좋은 피드백 예시:**
- ✅ "useDeferredValue를 활용하면 검색 입력과 목록 렌더링을 분리할 수 있습니다"
- ✅ "React Compiler의 한계인 동적 의존성 케이스를 언급했다면 더 깊은 이해를 보여줄 수 있었습니다"

**피드백 작성 규칙:**
- 개선점을 제시할 때는 **반드시 구체적인 기술/개념/코드를 언급**
- "~할 수 있다"가 아니라 "~를 하세요" 또는 "~가 빠졌습니다"
- 실무에서 시니어가 주니어에게 하는 코드리뷰처럼 작성

---

## Output Format
Return ONLY valid JSON:
{
  "score": number (0-100),
  "grade": "PASS" | "BORDERLINE" | "NEEDS_WORK",
  "analysis": {
    "situationUnderstanding": "상황/제약조건을 얼마나 정확히 파악했는지 (1-2문장)",
    "solutionQuality": "제시한 해결책의 적합성과 실현 가능성 (1-2문장)",
    "technicalAccuracy": "기술적 정확성 평가 (오개념이 있다면 명시)",
    "depthOfThinking": "설명의 깊이와 구체성 수준. concept 유형은 '원리 설명의 깊이', tradeoff 유형은 '대안/장단점 분석 수준' 등 질문 유형에 맞게 평가 (1-2문장)"
  },
  "matchedPoints": ["충족한 핵심 포인트들"],
  "missedPoints": ["놓친 핵심 포인트들. 100점이면 반드시 빈 배열 []"],
  "strengths": ["답변의 강점 1", "강점 2"],
  "improvements": ["100점 = 빈 배열 []. 90-99점 = 아쉬운 부분 1개. 80-89점 = 부족한 부분 1-2개. 80점 미만 = 핵심적으로 놓친 부분"],
  "feedback": "면접관 종합 피드백 (3-4문장, 한국어 존댓말). 100점이면 칭찬만, 90-99점은 칭찬 위주 + 가벼운 보완점, 80-89점은 균형있게, 80점 미만은 부족한 점 중심",
  "betterAnswer": "100점 = 빈 문자열. 99점 이하 = 더 나은 답변 방향 제시"
}`
}